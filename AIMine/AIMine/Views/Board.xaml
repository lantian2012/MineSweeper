<UserControl
    x:Class="AIMine.Views.Board"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIMine.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:AIMine.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="640"
    d:DataContext="{d:DesignInstance Type=vm:GameViewModel}"
    d:DesignWidth="640" Loaded="UserControl_Loaded_1">
    
    <Grid>
        <Grid x:Name="BoardGrid" HorizontalAlignment="Left" Height="640" VerticalAlignment="Top" Width="640"/>

    </Grid>
</UserControl>
